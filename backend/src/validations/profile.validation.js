const { z } = require("zod");
exports.updateProfileSchema = z.object({ full_name: z.string().trim().min(2).max(100).optional(), username: z.string().regex(/^[a-zA-Z0-9_]+$/).min(3).max(30).optional(), headline: z.string().max(160).optional(), bio: z.string().max(2000).optional(), location: z.string().max(100).optional(), website: z.string().url().optional(), phone: z.string().max(30).optional(), avatar_url: z.string().url().optional(), cover_image_url: z.string().url().optional() }).strict();
